#include<stdio.h>
int main()
{
	int a[100],i,n;
	printf("Enter size in array");
	scanf("%d",&n);
	printf("\nEnter elements in array: ");
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	printf("\nReverse of 1-Dimension array = ");
	for(i=n-1;i>=0;i--)
	{
		printf("%d\t",a[i]);
	}
	return 0;
}
