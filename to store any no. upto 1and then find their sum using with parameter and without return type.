#include <stdio.h>
void sum(int);
int main()
{
    int a;
    printf("\nNumber is: ");
    scanf("%d",&a);
    sum(a);
    return 0;
}
void sum(int x)
{
    int i,sum=0,y;
    for(i=1;i<=x;i++)
    {
        y=i;
        sum=sum+y;
    }
    printf("\nSun=%d",sum);
}
